// Modèle de configuration — À COPIER en `config.local.js`.
//
//   cp config.sample.js config.local.js
//
// `config.local.js` contient les identifiants réels du playground et n'est
// PAS versionné (voir .gitignore). Renseigner les valeurs ci-dessous dans la
// copie locale ; les diapos les injectent automatiquement (voir index.html).

window.SLIDE_CONFIG = {
  // Adresse du playground (ex. « http://192.0.2.10:8000 »).
  // Laisser vide pour afficher « lien fourni en séance ».
  PLAYGROUND_IP: '',

  // Mot de passe de connexion au playground.
  // Laisser vide pour afficher « (fourni en séance) ».
  PLAYGROUND_PASSWORD: '',

  // Adresse de contact ENSO (Paris 1 / Sorbonne Alliance).
  // Laisser vide pour afficher « nous contacter ».
  ENSO_EMAIL: '',
};
